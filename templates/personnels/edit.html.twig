{% extends 'base.html.twig' %}

{% block title %}Modifier L'agent{% endblock %}

{% block body %}
    {% include 'beneficiaires/_menu.html.twig' %}
    <main id="main" class="col-12 col-sm-12 col-md-12 col-lg-9 ps-md-2 pt-5 mb-5">
        <div class="container">
            <div class="row">
                <div class="col-8 col-sm-8 col-md-6 col-lg-8 col-xl-8 offset-lg-2">
                    <h1>Modifier L'agent</h1>
                    {{ include('personnels/_form.html.twig') }}
                    {{ include('personnels/_delete_form.html.twig') }}
                    <a href="{{ path('app_personnels_index', {'personnel': personnel.id }) }}" class="btn btn-warning my-2">Annuler</a>
                </div>
            </div>
        </div>
    </main>
    {% include 'beneficiaires/_footer.html.twig' %}
{% endblock %}
